<div class="row mt-4">
  <div class="col-md-4">
    <div class="card bg-secondary text-white mb-3">
      <div class="card-header text-center">Name: {{user.firstName}}</div>
      <div class="card-body">
        <h5 class="card-title">Email: {{user.email}}</h5>
        <p class="card-text">The best person in the world is you, {{user.firstName}}</p>
      </div>
    </div>
  </div>
  <div class="col-md-8">
    <div class="jumbotron bg-light text-center">
      <p class="lead">This is where the users are directed after they have logged in or clicked on a dashboard link.</p>
      <form action="/products/dashboard/roster/" method="post">
        <div class="form-inline justify-content-center">
          <fieldset>
            <legend class="text-center">{{title}}</legend>
            <div class="form-group">
              <input type="text" class="form-control mr-2" name="title" id="inputTitle" placeholder="Title">
              <button class="btn btn-outline-primary">Create Rosters</button>
            </div>
          </fieldset>
      </form>
    </div>
  </div>
</div>
{{> rostertiles}}
</div>